# Commits that only moved whitespace around. `git blame` should look straight
# through these to the change that actually wrote the line.
#
# Enable it once per clone (it is a local setting, not something the repo can
# turn on for you):
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically and needs no configuration.
#
# One full-SHA per line. Short SHAs are silently ignored, which looks exactly
# like the file not working.

# Format the codebase with ruff — no behaviour change.
1f751676c0585badb39eaab96791d8dd5d0237f5
